import { Person } from "@mui/icons-material";
import { SvgIcon, SvgIconProps } from "@mui/material";

const TruckFast = (props: SvgIconProps) => {
  return (
    <>
      <Person />
    </>
  );
};

export default TruckFast;
