import { PostAdd } from "@mui/icons-material";
import { SvgIcon, SvgIconProps } from "@mui/material";

const CartX = (props: SvgIconProps) => {
  return (
    <>
      <PostAdd />
    </>
  );
};

export default CartX;
