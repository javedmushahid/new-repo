import { School } from "@mui/icons-material";
import { SvgIcon, SvgIconProps } from "@mui/material";

const CartCheck = (props: SvgIconProps) => {
  return (
    <>
      <School />
    </>
  );
};

export default CartCheck;
